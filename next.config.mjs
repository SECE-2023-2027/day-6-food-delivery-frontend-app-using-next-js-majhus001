/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "cdn.pixabay.com",
      "img.freepik.com",
      "www.pexels.com",
    ],
  },
};

export default nextConfig;
